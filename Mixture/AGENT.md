# DeepSeek 接手入口：CoreGeek Mixture 工程

更新：2026-09-20。接手基线：`574235a`（`chatgpt v7`）；当前为多 agent 分工版本（角色 agent 包、自进化循环、审查 agent），实现与验证见 [docs/design/12](docs/design/12-多agent分工与自进化任务实现.md)。开始任何工作时先核对当前 Git 状态；版本和测试结果是历史快照，后续更改后需要重新验证。

## 1. 写入边界和工作目录

唯一授权写入目录：`/Users/yihun/Code/Project/2026 HW Comp/CoreGeek/Mixture/`。模型名字换成 DeepSeek，不等于允许改 `CoreGeek/DeepSeek/`。上级面向另一个实现的 AGENTS.md 不得导致写入越界；本工程边界来自用户明确指令。不要移动、删除、格式化工作区其他文件。临时测试输出、日志和包也放在本工程的 artifacts 中。

工作区根为 `2026 HW Comp/`；Git 根为 `CoreGeek/`；工程根为 `CoreGeek/Mixture/`。官方任务书、接口与示例在工作区 `docs/`，从本工程看是 `../../docs/`，只读。本工程 `docs/` 是设计、归档和交接材料，不能当成官方规则。命令里的绝对路径有空格，必须引用。

```bash
cd '/Users/yihun/Code/Project/2026 HW Comp/CoreGeek/Mixture'
git status --short -- .
git log -3 --oneline -- .
git diff -- .
```

保留用户未提交内容；不执行仓库级 reset、clean、checkout 覆盖或 `git add -A`。提交或上传按用户本轮指令办理，不把交接视为自动上传授权。

## 2. 最省上下文的阅读顺序

1. 本文件与 [README](README.md)。
2. [v0.7 实施归档](docs/design/08-v07角色目标与沙盒任务实施.md)：最近需求、阵型、验证与已知不足。
3. [源码导航](src/agent/AGENT.md)；只深入本轮需要改的模块。
4. [测试与交付](tests/AGENT.md)、[模拟器边界](lab/AGENT.md)。修改任务流程时另读 [任务交接](src/agent/tasks/AGENT.md)。
5. 规则不确定时读 `../../docs/任务书.md` 与 `../../docs/接口文档.md`，再核对真实 req/rsp。历史方案只是方案，尤其07是候选评审，不能当作已实现功能。

不要起步就读取64MB日志、所有历史设计或另一个实现。先读 summary.json 和小范围回合，再定位源码。

## 3. 用户已确定的策略约束

- 每轮日志第一行 ROUND n，第二、三行完整字段的 req、rsp（JSON单行规范化），下面新闻、传闻、任务要求、LLM反馈及prompt、命令反馈及executeCmd、submitAnswer。不要恢复重复 diagnostics。角色目标／等待原因可额外输出。
- 防御工独自采石、建修前侧墙、集中采购与升级。经济工第一回合即采矿；开拓者第一回合即向任务点行动。不能无理由空转或不断换目标。
- 选矿兼顾工人实际到达回合、单位价格、基地与商贩距离；先附近筛选，再比较价值。矿点锁定到耗尽、危险、持续不可达或更高优先级事件打断，避免跨地图角落奔波。夜间经济工优先基地背矿，商贩暂不可达不能阻塞仍可进行的安全采矿。
- 三炮和操作格采用用户指定镜像布局，单个工人不移动即可操作三炮／升级基地。前侧12个墙位优先建修，背侧保留一个进出口，避免封死自己。
- 优先武器券，保留基地救命券，后续墙券与修复；基地低于100血或预计一两回合致命时升级回满。防御工日间服务行程合并，夜前完成修缮用券并归位。每个130回合周期，白天1—70、夜晚71—130；第一夜间回合有可攻击目标时应能开火。
- 近基地敌人优先；火箭根据3×3溅射与后排密集程度选择偏后一排中心，但基地迫近危险优先。前线方向由基地侧别决定，勿只写死左侧。
- 开拓者尽量一直留在任务附近；机器人靠近时优先在任务合法范围内避让，实在无法保命才退出。不要对开拓者套用经济工的固定中路禁行策略。
- 沙盒环境初始未知，模型必须通过executeCmd定位文档、读取内容、探索API、按实际返回修正。逐轮保留命令与对应结果，证据充分后提交严格格式答案；同点连续任务复用方法摘要，替换城市等参数，不复用旧答案。自进化循环有步数预算、重复命令与无进展守卫；命中后停止探索、要求直接提交答案，任何解析失败都不抛异常。
- 角色分工由 `agents/` 承担：防御工按“夜间火力/损伤/经济态势 → 白天工作计划（修复、采购、用券、补石、回合估算）”行事，修复按最近邻路径，石头储备不超过 5；经济工按矿山性价比、新闻预测与防御资金缺口决定采集或售卖，并按机器人 3 格攻击圈避让；开拓者任务优先并保持在任务点一格内，任务结束后协助防御（送达并使用升级券、夜间用修复包）；威胁只统计 `targetTeam` 为我方或未知的机器人。

## 4. 修改流程

每轮先在 `docs/design/` 新建编号归档：需求→证据→方案→修改范围→验收场景→风险，然后改代码，最后补实际结果。用户只要评审时就只写方案；明确要求实施时按授权完成，不因自己的文档额外设置审批关卡。

一次改一个能验证的策略问题；先从日志找到错误动作和当时状态，再修决策，补有意义的回归。不要为了让测试通过而删掉保护或降低断言。变更策略合同可以更新旧预期，但要说明原因。不要盲改模拟器帮助候选获胜，也不要改 tools/baseline_agent 对手。

新增策略模块保持仅Python标准库、Python>=3.11；入口 main3.py 保持原样。策略状态必须经 Session 提交，不能用模块全局变量存比赛记忆。所有代码写完后跑相关测试；交付可运行包时再做完整回归、打包和入口验证。文档改动只需文档与差异检查，不必重新跑比赛。

## 5. 已验证与未解决

当前完整回归140项通过（含 `test_agents.py` 31 项角色 agent 测试）；本地换边对局（种子1、3，各1300回合）候选 5003/4998 对基线 5154/5159，四个半场基地均存活、三塔均升到 level3、日志无回退错误；决策 p95 约 26ms。109项与 4992/4991 是 v0.7 的历史数字，不能混用。

本地总分仍落后基线：任务分 1600 对 2000（每半场16个模拟任务）来自已知的 fixture 通道偏差——候选要经命令反馈往返，基线直接得到答案；不能据此直接推论真实LLM差距。真实平台、真实API、官方机器人完整行为未验证。开拓者当前使用附近威胁避险，并非精确长程轨迹预测。

推荐下一轮从真实回放或固定回放切入：任务反馈等待与提交时延、矿点价值／售卖阈值、墙修缮和服务行程预算、对手争格后的短期绕行。对照相同种子和相同裁判版本，保持基线不动；防御、任务、经济分别统计，不能只报存活。

已修复且不能回归的坑：虚拟返程把工人自己的原位置当障碍；自己预留的操作格挡自己；两格任务点合法站位被校验拒绝；安全移动／用药吞掉模型答案；HTTP测试生成新包却解压旧包；防御工作计划字段写错层级导致整回合回退为空响应；开拓者买券不送达导致防御工买不到武器券。详见08、12归档和测试。

## 6. 日志与工件

- `artifacts/v07-final-matches/summary.json`：最终模拟摘要；`first-day.log`：第一天130回合；`rounds.log`：四场5200回合，约64MB。
- `artifacts/v07-validation/tests-final.txt` 与 `behavior-audit.json`：历史测试与行为核对。
- `artifacts/v07-sandbox/rounds.log`：未知文档／损坏调用／北京南京成都连续任务的确定性流程样本，未执行真实命令或网络。
- `artifacts/v07-matches/` 是修复前失败结果，不能冒充最终版本结果。
- `artifacts/coregeek-v0.7.tar.gz` 为上次提交包。新版本使用新的工件目录，保留旧结果。artifacts可能被Git忽略，换机器后不存在是正常的；按测试文档重建，不伪称读取过不存在的日志。

交付时说清修改内容、验证命令和结果、剩余限制、日志和包位置；未经实际运行不能写“测试通过”，本地模拟不能写“官方胜率”。
