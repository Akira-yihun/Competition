# 本工程 Agent 规则入口

用户授权写入仅限 `/Users/yihun/Code/Project/2026 HW Comp/CoreGeek/Mixture/`，包括代码、文档和测试工件。其他区域只读；换用 DeepSeek 接手不改变此边界，不要转到同仓库 DeepSeek 目录修改。

**开始工作必须阅读 [AGENT.md](AGENT.md)**，其中定义阅读顺序、角色策略、修改与验证流程、当前基线及已知问题。该文件是本工程交接正文，避免在两个入口复制整份内容而失去同步。

任务涉及子目录时，按正文链接读取相应 AGENT.md；这些文件不依赖工具自动加载，需显式打开。每轮先写优化方案，再实施与记录结果。保留用户改动，不覆盖历史归档；用户最新明确指令优先。
