# 迭代报告 `<iterId>`

> 由 `loopctl iter analyze` 生成。**纪律：没有证据的结论不许出现在本报告里。**

## 1. 结论
- 决策：`accept | revert | need_more | inconclusive`
- 一句话理由：
- 置信度：`高 | 中 | 低`（样本数与区间见 §4）

## 2. 合规检查表（任一不过 ⇒ 直接 revert，不看分数）
| 检查 | 基线 | 本迭代 | 阈值 | 结果 |
|---|---|---|---|---|
| 异常次数（`errors[]` + 判定） |  |  | ≤ 0 |  |
| `guard` 拦截率 |  |  |  |  |
| 决策耗时 p50 / p99 / max (ms) |  |  | p99 ≤ 3000 |  |
| 兜底触发次数 |  |  | 0 |  |
| 日志完整性 `fwlog verify` |  |  | ok |  |
| 打包可运行（`bash run.sh <port>`） |  |  | ok |  |

## 3. KPI 对比
| 指标 | 基线 | 本迭代 | 历史最好 | Δ | 备注 |
|---|---|---|---|---|---|
| `score_total` |  |  |  |  |  |
| `score_1`（任务） |  |  |  |  |  |
| `score_2`（击杀） |  |  |  |  |  |
| `score_3`（生存） |  |  |  |  |  |
| 半场胜率 |  |  |  |  |  |
| 击杀数（小/中/大/BOSS） |  |  |  |  |  |
| 任务完成率 / 平均耗时 |  |  |  |  |  |
| 基地掉血速率（HP/晚） |  |  |  |  |  |
| 金币曲线（第 3/6/10 天） |  |  |  |  |  |

## 4. 统计证据
- 样本：`<n_matches>` 场 = `<n_halves>` 个半场；对照方式：`配对 | 独立`
- 主指标区间：`[低, 高]`（方法：Wilson / bootstrap，α=0.1）
- 检验：`p = <...>`（配对时给出符号检验/秩和）
- 噪声地板（来自 `loopctl calibrate`）：`<...>`
- 结论强度：`显著改善 | 不显著 | 显著恶化`

## 5. 归因 Top-5（每条必须带可追溯证据）
| # | 现象 | 证据（事件 id / 字段） | 估计影响（分） | 归属假设 |
|---|---|---|---|---|
| 1 |  |  |  |  |
| 2 |  |  |  |  |

> 后悔值（regret）来源：`decision` 事件里的 `cands[].ev` 与下回合 `outcome` 的差值，按 `module` 聚合。

## 6. 未解释现象（**必须显式列出**）
- 
- （空着也行，但如果日志里有异常/兜底/非法指令而未列入本节，视为报告不合格）

## 7. 下一轮建议假设（3 条，按预期收益排序）
| # | 假设 | 预期收益 | 依据 | 对应 backlog id |
|---|---|---|---|---|
| 1 |  |  |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |

## 8. 附录
- 产物：`runs/<iterId>/`
- 平台：`build_id=<...> matches=<...>`
- 日志：`logs/<matchId>/raw/*.jsonl`（`fwlog verify` = `<ok/gap...>`）
