# 《未来战争》Demo（Demo/CoreGeek）代码优化分析

> 依据：`docs/任务书.md`、`docs/接口文档.md`、`docs/request.txt`、`docs/response.txt`
> 方法：逐行审阅 + 用 `docs/request.txt` 实际跑 `decide()` 做行为验证（文中"实测"均为本机真实输出）

---

## 0. 结论速览

Demo 是一个**无状态、单目标、只覆盖了协议 1/3 动作集**的基线 Agent：

```
main3.py  →  server.py (HTTP)  →  brain.decide(payload)  →  protocol.Turn.load / grid.next_step
```

现有策略回路只有一条：**挖矿 → 攒够 12 个再卖给小贩 → 造 3 座 1 级塔（固定塔位）→ 用石头围一圈围墙（固定环）**。
它丢掉了任务书里绝大多数得分手段：

| 得分线 | 满分 | Demo 现状 |
|---|---|---|
| `score_1` 任务（推理类/长上下文类/自进化类） | 每任务 50 + 速度加成，另有大额宝藏 | **几乎为 0**：只用一次 LLM 原文提交，不用沙盒 `executeCmd`，不做宝藏 |
| `score_2` 击杀 | 随天数增长无上限 | 低：`gatling` 目标不足时整回合不开火、`railgun` 只打 1 个、`rocket` 三个落点完全重叠 |
| `score_3` 生存 | **550**（∑10×day） | 部分：围墙环实测挡不住任何伤害，塔永远 1 级 |

风险面：任务书第八章规定**累计 5 次异常直接终局**，而 Demo 没有任何响应自检、没有反馈闭环、
没有异常隔离，属于"能跑但脆"的代码。

下面按 **P0 健壮性 → P1 协议覆盖 → P2 策略 → P3 算法/工程** 四层给出问题与改法。

---

## 1. P0 正确性与健壮性（不修可能直接输）

### 1.1 完全无状态：跨回合信息全丢
`decide()` 每次从零解析 payload（`brain.py:33`），进程内没有任何记忆。而本游戏是**跨回合长程博弈**，下列信息
只能靠记忆获得，Demo 全部放弃：

- 昼夜相位、距天黑还有几回合（`Turn.is_day` 由 `(roundNo-1)%130<70` 现推，`protocol.py:134`）
- 任务状态机：已接任务、已提交答案、通过率、剩余超时回合、任务点冷却
- 沙盒会话：`executeCmd` 的输出在**下一回合**的 `lastCmdResult` 才回来 → 必须跨回合续跑
- 民间传闻是 `DAY1/DAY2/...` 逐日累积的 → 必须逐日拼接才能推断宝藏位置
- 已探明的可建造/不可建造格子（见 1.3）
- 历史价格（`vendorShopList`）与官方新闻，用于囤货/抛售

**改法**：引入 `state.py`，按 `teamId` 建立单例 `GameMemory`（`round_no`、`phase`、`task`、`prices[]`、`news[]`、
`buildable_map`、`answers{}`、`sandbox`），每回合开头 `memory.update(payload)`，决策只读 memory。
注意配合 1.5 的并发锁。

### 1.2 没有响应自检 → 踩"5 次异常"红线
`decide()` 的返回值直接序列化发出（`server.py:19`），没有任何校验。而任务书第八章明确：
**"指令错误"= 字段缺失或指令无法被识别**，累计 5 次即终局；「执行失败」（如移动碰撞、落点无目标）才不计异常。
典型风险：

- `build_command(target, "wall")`（`brain.py:110`、`protocol.py:244`）——`wall`/`gatling` 这些名字是**猜的**，
  接口文档只给了动作码，没给 `build` 的 `name` 取值表。若判题器不认，每回合都在制造"指令错误"。
- `attack` 的 `targetPos` 数量必须等于**当前武器等级**（加特林/火箭，`targetPos` 数 = 等级；电磁只有 1 个）。
  Demo 用 `max(tower.level,1)`，一旦 payload 缺 `level` 字段就发错数量。
- 白天发 `attack`、非工人发 `build`、`use` 眩晕法宝/炸弹漏 `targetPos` 等。

**改法**：新增 `guard.py::validate(response, turn)`，在序列化前逐条检查：
角色 ID 属于我方 → 每角色至多 1 条 → 动作码在白名单 → 必填字段齐全 → `targetPos` 在界内/数量匹配 →
昼夜与角色类型约束。校验失败**丢弃该条**（丢 1 条指令不算异常），并把校验事件写日志。
这是投入产出比最高的一个改动。

### 1.3 从不读 `lastRoundRoleActionResults` → 唯一反馈通道被浪费
`Turn.load` 根本没解析该字段（`protocol.py:127-162`）。它是判断**指令合法性**与**探索环境**的唯一手段：

- 探明**可建造区域**：任务书 4.1 规定蓝区只能造武器、黄区只能围墙，但**报文里没有这个区域信息**。
  Demo 只判 `turn.land(pos)`（`protocol.py:214`），会把围墙/武器造到非法格子，且**每回合重复犯同样的错**。
  正确做法：维护 `buildable_map: dict[(x,y,kind), bool]`，用 `lastRoundRoleActionResults[id]==False` 把该格永久标记为非法并换点。
- 探明 `build` 的 `name` 取值是否正确（若某种名字持续 false，切换候选词并告警，而不是眼睁睁吃 5 次异常）。

### 1.4 任务状态机缺失导致"接了任务立刻作废"
任务书第五章：**离开任务点周围一格即视为任务结束**。而 `_night()` 无条件把每个角色拖向武器
（`brain.py:149-160`）。实测 round 71（入夜第一回合）开拓者 `10011` 被指派从 (10,12) 走向 (9,13)，
即**开局第一晚就把自己的任务作废**。同理，夜间接的任务也会被作废。

**改法**：任务期间开拓者的位置是硬约束（必须留在任务点 1 格内），夜间调度需把它从"操纵武器"名单中排除，
只让 2 个工人去操作 3 座塔（或按 4.3 的价值排序决定牺牲哪座塔）。

### 1.5 兜底与并发
- `server.py:20-22` 捕获所有异常后返回 `{"roleCommandMap":{}}`：不会崩，但**整回合报废**，
  且掩盖 bug（只留一行日志）。应改为"降级但不能空"：先返回一个**保守安全指令**（例如所有单位原地不动/回基地方向移动），
  并把 traceback 落盘。
- 现在是 `ThreadingHTTPServer` + 无共享状态，一旦按 1.1 引入记忆，就必须加 `threading.Lock`（并考虑按 `roundNo`
  做幂等：判题器重发同回合请求不能重复推进状态）。
- 建议加"决策看门狗"：`decide` 超过 3s 直接返回上回合的安全指令（判题器 5s 超时红线）。

### 1.6 打包与运行面
- 接口文档规定入口是 `bash run.sh port`，**Demo 没有 `run.sh`**（`CoreGeek.tar.gz` 里也没有），提交前必须补：
  `#!/usr/bin/env bash` + `exec python3 main3.py "$1"`，并处理 `port` 缺省/非法。
- `main3.py:16-20` 把 INFO 日志打到 **stdout**，且每回合打印整条响应。若判题器把 stdout 接管道且不读，
  1300 回合后管道缓冲区写满会**阻塞进程 → 全回合超时**。建议日志改到文件（`logs/agent.log`），
  stdout 只保留启动行，或按大小轮转。
- `grid.py:51-55` `_first_step` 在 `goal == start` 时会 `KeyError`。当前调用路径恰好规避了，
  但属于埋雷，建议 `next_step` 开头直接 `if goal == moving.pos: return None`。
- 无任何测试。至少要有一个"用 `docs/request.txt` 跑一遍 + schema 校验 + 黄金响应比对"的 smoke test。

---

## 2. P1 协议覆盖缺口（把该拿的分拿回来）

`grep` 结果：`buy` / `use` / `executeCmd` / `worldNews` / `vendorShopList` / `summonTreasure` /
`lastRoundRoleActionResults` 在整个 `src/` 里**一次都没出现**。逐项列出后果：

| 报文/动作 | Demo 现状 | 后果 |
|---|---|---|
| `executeCmd` + `lastCmdResult` | 永远 `""`（`brain.py:45`） | 自进化类任务（任务书 5.3：给 API 文档让你查天气）**必须先探索沙盒**。不执行命令就只能瞎猜，`score_1`≈0 |
| `prompt` / `llmResp` | 一次性问答，把**LLM 原文整段**当答案提交（`brain.py:68-75`） | 答案含解释/Markdown，必然 `errorCode 2`；且一旦 `llmResp` 非空就**每回合重复提交同一答案**，浪费全部任务回合 |
| `buy` | 从未调用 | 金币是废纸：塔永远 level1、没有升级券、没有消耗品，`score_2/3` 上限被锁死 |
| `use` | 从未调用 | `WeaponUpgradeVoucher1/2`、`StationUpgradeVoucher`、`WallFixer`、`Medicine`、`Bomb`、`DizzyWeapon` 全部未用。炸弹 100 金 = 3×3 内 100 伤害，性价比远高于塔的输出 |
| `summonTreasure` | 从未调用 | 长上下文类任务（宝藏=大量积分+金币）直接放弃 |
| `worldNews` | 未解析 | 推理类任务（5.1 官方消息 → 矿价涨跌、某矿停采 2 天）收益为 0 |
| `vendorShopList` | 未解析 | 卖矿只看"谁数量多"（`brain.py:117-118`），**不看价格**。铜基础价 5 > 铁 3 > 石 1，新闻会把某矿价格拉高，Demo 完全无感 |
| `weaponShopList` | 未解析 | 无法按价格决策买什么 |
| `robot.abnormalState` / `targetTeam` | `Robot` 只解析 id/pos/health（`protocol.py:101-109`） | 不知道机器人**打谁**（会浪费火力去打正走向敌方基地的机器人），也不知道谁被眩晕（眩晕 5 回合内不构成威胁） |
| `PlayerTask.timeoutRounds/coldDownRounds/taskType` | 只取 `isValid`（`protocol.py:24-29`） | 无法做"剩余回合够不够做完"的接任务决策，也无法规划 30 回合冷却的节奏 |
| `errors` / `lastSummonTreasureResult` / `totalScore` | 未解析 | 无自诊断、无局势感知 |
| `Role.attackPower` | 未解析（用了硬编码表 `TOWER_RANGE_BY_LEVEL`） | 应以报文为准，硬编码表只做兜底 |

**优先级最高的两个**：
1. **自进化任务的 SOP/Skill 闭环**（拿分最大）：`acceptTask → prompt(任务原文 + 记忆中的 SOP + 上回合 lastCmdResult)
   → 解析 LLM 输出为 {executeCmd | submitAnswer} → 下一回合读 lastCmdResult 继续 → 成功则把这次成功路径固化成 SOP 复用`。
   `executeCmd` 有 15s / 64KB 限制，**必须跨回合串行推进**，所以要状态机 + 超时预算（`timeoutRounds`）。
2. **经济闭环**：`collect → sell(按价格) → buy(升级券/炸弹) → use(升级/使用)`。

---

## 3. P2 策略层缺陷（得分效率）

### 3.1 防御：19 格石头的围墙环，实测 0 防护
`_wall_order()`（`brain.py:240-254`）在基地外**偏移 2 格**围一圈（实测基地 (10,23)-(11,24) → 环在偏移 2 处，19 格，留 1 个缺口 (13,22)）。
实测结论：环外可站立的 29 个格子**全部**都在基地 3 格以内（机器人攻击距离 = 3），
即机器人贴着围墙就能**隔着墙打基地**，这圈石头只挡住了"走进来"，没挡住伤害。

- 几何规律：外圈偏移 `d`，则贴墙机器人到基地距离 = `d+1`。要躲开射程 3，需要 **`d ≥ 3`**。
- 建议：**外圈偏移 3～4 格 + 无缺口分层**（或按进攻轴做卡口），把机器人挡在射程外强拆墙；
  这同时给塔创造了"打墙外靶子"的安全输出位。
- 代价：环越大石头越多（每格 1 石头），需与"西侧采石"节奏联动。

### 3.2 塔位固定且严重偏置
`_tower_sites()`（`brain.py:230-237`）取基地 1 格邻域按 `(footprint_distance, x, y)` 排序取前 3，
即**永远取 x 最小的 3 格**。实测 = `(9,22) (9,23) (9,24)`，三座塔全挤在基地西侧，
北/南/东三个方向零覆盖（虽然加特林/电磁的射程能覆盖一部分，但覆盖中心明显偏移）。
且塔位在初始化后永不变化，既不随机器人来向调整，也不考虑"操纵者能否站得下"。

**改法**：塔位评分 = 对"来向扇区覆盖率 × 机器人路径密度"打分，并保证 3 座塔的**扇区互不重叠**；
留出操纵站位；优先把 1 号塔放在**唯一必经通道**上。

### 3.3 火力分配：三种武器全部次优（均已实测）

- **加特林等级 2/3：锥内目标不足 `level` 个时直接不开火 → 整回合 0 输出**（`brain.py:188-189`）。
  实测 level2 塔配 4 个射程内机器人，输出 `None`（无指令）。
  - 修：把"同象限"换成**显式 90° 锥搜索**——枚举每个候选机器人为锥心，取 ±45° 内目标，按期望伤害打分取最优；
    锥内实在不足时才考虑重复落点（同一落点合法且角度 0）。
  - 进阶：加特林子弹"沿弹道命中最近机器人即消耗"，所以**瞄 A 可能实际打中更近的 B** → 应按弹道线（而非点）选靶。
- **电磁狙击炮只打 `ordered[0]`**（`brain.py:180`）。实测 3 个机器人排成一条线 (9,27)(9,28)(9,29) 时，
  只指定了 (9,27)，能量 30 只造成 40 伤害；沿该线可穿透 3 台，伤害翻倍。
  - 修：枚举候选方向/直线，最大化 `Σ min(残余能量, hp)`，优先选"能在本回合完成击杀"的线。
- **火箭 level3 三个落点完全重叠**（`brain.py:177` 返回 `[target]*level`）。实测输出 `[{9,28},{9,28},{9,28}]`。
  - 修：落点集合做小规模伤害最大化（网格 3×3 邻域内的候选簇 → 贪心/DP 选 `level` 个落点，
    中心 20、溅射 10、重叠叠加），在"覆盖多簇"和"叠杀高血目标"之间取最优。
- **目标函数** `threat = (基地距离, health, id)`（`brain.py:172-174`）：
  - 没有**击杀优先**（小机器人 40 血，1 发 10 伤害；应优先补齐能打死的目标，把 `score_2` 拿到手）；
  - 没有**过量伤害控制**（3 发全砸 BOSS 800 血是浪费）；
  - 没有按 `targetTeam` 过滤（在打的可能不是我们）；
  - 没有用 `abnormalState == "dizzy"`（眩晕目标可以降权）。
  - 注意：结算顺序是"武器攻击 > 机器人移动"，所以**不需要预判提前量**，按当前位置打即可（这点 Demo 做对了）。

### 3.4 角色↔武器配对用 `zip`（`brain.py:163-164`）
`zip(controllable(), weapons())` 按 ID 序 / 坐标序硬配，不做距离匹配。基地周围三塔、三个角色散落各处时，
会产生明显次优（多走 N 回合 = 少 N 回合输出）。3×3 规模直接枚举全排列取最小总距离即可。

### 3.5 昼夜节奏：白天不预置，夜里才开始跑
`_night()` 才让角色走向武器；白天角色可能在几十格外挖矿，入夜后要花多回合赶路（且途中被机器人打）。
**建议**：白天按"距天黑剩余回合 × 距离"提前返场（例如天黑前 `ceil(dist/1)+2` 回合归位），
并把"最后 N 回合不做远距离任务/长途拉矿"写进日程表。

### 3.6 经济：只会卖最多的矿，从不买东西
- `_sell_if_ready()`（`brain.py:115-130`）阈值 `count>=12` 或背包满才去卖，**且与价格、与新闻无关**。
  应改为"按价格排序卖 + 预期涨价的矿先囤"（官方新闻明确给出 2 天停采/涨价窗口）。
- 从不 `buy`：应以 **275 金把 1 座塔推到 level3**（火箭 level3 = 全图射程 + 60×3 伤害）为经济目标，
  或者买 `Bomb`（100 金 / 3×3 内 100 伤害）做清场。Demo 的金币完全沉淀。
- 采集策略：`_mine` 用 `claimed` 独占矿点（`brain.py:140`），但规则允许**多工人共享同一矿**
  （"矿石不足平分时每个工人各得 1"），独占会人为降低采集效率；应在"共享高价值矿"和"分散找矿"之间按价值决策。

### 3.7 胜负意识：只想着活，没想过赢
胜负判定是**基地先被摧毁者负**。火箭 level3 射程"全图"且导弹"指哪打哪不被阻挡"——理论上可以从自家基地
隔着全图拆对面基地。Demo 的塔位/目标选择完全没有"进攻敌方基地"这条线（甚至 `teamEnemy` 里的敌方基地位置
解析了却从不使用）。这是夺冠级策略空间，值得在仿真里验证。

### 3.8 任务领取决策过于简单
`_pioneer_day()`（`brain.py:63-87`）取 `score+gold` 最大、距离最近的任务点，不看 `timeoutRounds`、
不看昼夜剩余、不看任务点冷却（30 回合）。应做"完成概率 × 收益 / 耗时"的效用排序，并保证
"接了就一定原地做完（含跨夜）"。

---

## 4. P3 算法与工程

### 4.1 寻路：每个目标跑一次 A*，应改成每回合一次距离场
`_step_toward` 对每个候选站位调一次 `next_step`（最多 8 次/角色/回合，`brain.py:212-220`），
而 `next_step` 内部每次重建 `blocked`（`protocol.py:227-233` 遍历全部 zones+units+robots）并做启发式搜索。

**改法**：
1. 每回合构建一次静态阻挡位图（`bytes`/`bytearray`，41×32=1312 字节），O(1) 查询；
2. 8 邻域等代价 → **一次多源 BFS 距离场**即可得到到任意目标的精确步数（当前 A* 的启发式是冗余的），
   或对"所有候选目标"做一次反向 BFS（`dist_map`），所有角色共享；
3. 用**保留表**替代散落的 `claimed` 集合（谁在哪回合占哪个格，含对角互换/同格争夺规则，任务书 4.5.4-5）。
   当前 `claimed` 语义混乱：既表示"移动落点"，又表示"矿点归属"（`brain.py:140`），还会漏掉"目标点争夺"的对称情形。

实测当前 `decide()` 延迟 **0.6–4 ms**（0–700 墙 / 0–400 机器人），**性能不是当前瓶颈**；
但一旦加入"距离场 + 落点打分 + 仿真推演 + 沙盒/LLM 状态机"，5s 响应红线才需要认真对待，
所以建议现在就定 3s 软预算 + 看门狗。

### 4.2 代码结构建议
```
src/agent/
  model.py      # 全字段协议解析 + 类型化指令构造（RoleCommand dataclass）
  state.py      # GameMemory：跨回合记忆 + 幂等
  perceive.py   # 威胁评估/矿区/价格/可建造图
  nav.py        # 阻挡位图 + 多源 BFS 距离场 + 保留表
  econ.py       # 采集/贩卖(按价)/购买/升级
  combat.py     # 建塔选址、围墙几何、配对、选靶（三种武器分别打分）
  quest.py      # 自进化任务状态机 + LLM/沙盒 SOP 自进化
  news.py       # 官方新闻(矿价/停采)与民间传闻(宝藏)解析
  guard.py      # 响应自检 + 降级兜底
  server.py     # HTTP + 锁 + 看门狗 + 落盘日志
  config.py     # 阈值/权重集中（便于仿真调参）
```
- 消除 `Any` 与魔法数：`_attack_targets(robot: Any)`、`SELL_BATCH/STONE_BATCH/TOWER_LOADOUT` 应进 config。
- `protocol.py` 里 `Unit.level` 对角色填 0（`protocol.py:78`）语义含糊，`range_of_attack` 又做 `max(level,1)` 兜底，
  容易掩盖"报文缺字段"，建议显式 `Optional[int]` + 缺失告警。
- `Turn.land()` 与 `blocked()` 的判定重叠（`blocked` 已含全部非 land 的 zone），可合并去重。

### 4.3 仿真与调参（长期收益最高）
Demo 零测试、零回放。建议：
1. 用 `docs/request.txt` + `docs/response.txt` 做 schema 级 smoke test；
2. 写一个**简化判题器**（昼夜、移动碰撞、武器结算、机器人波次、任务超时），把历史 payload 录制成回放集；
3. 在仿真上做参数搜索（塔型/等级、墙环偏移、卖矿阈值、返场提前量、选靶权重），
   并把"对手"设成当前 Demo 作为基线。策略类比赛里，**没有仿真就没有迭代速度**。

---

## 5. 迭代路线图（按性价比排序）

| 阶段 | 内容 | 预期收益 | 工作量 |
|---|---|---|---|
| **P0** | `guard.validate` 自检 + 降级兜底；`GameMemory` 状态存储（加锁、幂等）；用 `lastRoundRoleActionResults` 学可建造图与命令合法性；`run.sh` + 日志落盘；`next_step` 边界修复 | 消除被终局风险，止损"每回合重复犯错" | 0.5–1 天 |
| **P1** | 自进化任务状态机（`prompt`+`executeCmd`+`lastCmdResult`+SOP 固化）；`buy`/`use` 升级券与消耗品；按 `vendorShopList` 价格卖矿；解析 `worldNews` | 直接打开 `score_1` 与主要 `score_2` 增长曲线 | 2–3 天 |
| **P2** | 三种武器选靶打分器；角色↔塔最优配对；塔位与围墙几何重设计（外圈偏移≥3、无缺口）；昼夜返场节奏；开拓者留驻任务点 | `score_2`/`score_3` 显著提升 | 2–3 天 |
| **P3** | 距离场+保留表重构；模块化拆分；简化判题器 + 回放 + 参数搜索；宝藏/进攻敌方基地等长线策略验证 | 从"能打"到"最优化" | 持续 |

---

## 6. 需要向主办方确认的点（猜错就会吃异常或白干）

1. `build` 的 `name` 取值到底是 `wall/gatling/railgun/rocket`，还是中文名？（猜错 = 每回合"指令错误"）
2. 蓝色/黄色可建造区域是否强校验？是否会在报文中下发？（不在报文里就必须靠 `lastRoundRoleActionResults` 在线学习）
3. 机器人是否会**隔着围墙**用攻击距离 3 打基地？（决定围墙几何，见 3.1）
4. 同一回合同时下发 `prompt` 和指令是否允许？`prompt` 的计数是否只按"非任务期"扣减？
5. 回合与昼夜对齐：`roundNo=1` 是否为白天第 1 回合（Demo 按 `(roundNo-1)%130<70` 推，实测 round85 有机器人 →
   与"夜间"一致，基本可信）。
6. 空 `roleCommandMap` 是否计异常？同一角色重复 key 会怎样？`submitAnswer` 在无任务状态下提交是否算"指令错误"？
7. `executeCmd` 跨回合是否保持**同一沙盒会话**（环境变量/文件是否留存）？输出 64KB 截断如何处理？
8. 围墙/武器"被覆盖建造"的规则细节（4.5.1 提到新建覆盖已有武器），是否会返还资源？

---

## 7. 可直接落地的小修（低风险、立竿见影）

1. `next_step()` 开头加 `if goal == moving.pos: return None`（消除 `grid.py:51` KeyError 隐患）。
2. `decide()` 包一层 try/except，异常时返回**保守移动**而非空指令，并 `LOGGER.exception` 落盘。
3. 每天天黑前 `RETURN_MARGIN` 回合开始返场（`_day` 里加"距天黑回合数"判断）。
4. 任务期间把开拓者从 `_tower_pairs` 的可操纵名单里剔除（避免自废任务）。
5. `_attack_targets`：加特林锥内不足时，用最优锥搜索而不是 `return []`；电磁改成"选穿透收益最大的线"；
   火箭落点去重（先保证不浪费 3 发导弹）。
6. `_sell_if_ready`：把 `max(count)` 换成 `max(count * price)`（价格来自 `vendorShopList`）。
7. 解析 `robot.targetTeam`，只对"打我方"的机器人计威胁；`abnormalState=="dizzy"` 的目标降权。
8. 保留 `gold >= WEAPON_BUILD_COST` 的同时扣除**本回合已下单**的花费（当前两工人可能重复消费同一笔金币）。
9. 塔位/围墙目标点排除机器人所在格（当前只排除 `occupied`，机器人格子可能被选中 → 白跑一回合）。
10. 补 `run.sh`，日志落文件，stdout 只留启动行。
